version: '2'

services:
    python:
        image: python:3.6
        container_name: dice_python
        volumes:
            - .:/var/dice
        working_dir: /var/dice
        command: sleep 1000000
